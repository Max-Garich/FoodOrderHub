# Backend Configuration
PORT=3001

# REQUIRED: Generate a secure random string (e.g. openssl rand -hex 32)
JWT_SECRET=

# PostgreSQL (docker-compose). Generate: openssl rand -hex 16
POSTGRES_USER=foodorderhub
POSTGRES_PASSWORD=
POSTGRES_DB=foodorderhub
